# ArkTS Codegen

Future home for Reader UI Contract -> ArkTS type generation.

Output target:

```text
generated/arkts/
```

The generator must emit deterministic ArkTS types for route, event, state, view-state, motion, and token contracts.
