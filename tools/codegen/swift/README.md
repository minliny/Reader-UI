# Swift Codegen

Future home for Reader UI Contract -> Swift type generation.

Output target:

```text
generated/swift/
```

The generator must emit deterministic Swift types for route, event, state, view-state, motion, and token contracts.
