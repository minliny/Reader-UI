# Kotlin Codegen

Future home for Reader UI Contract -> Kotlin type generation.

Output target:

```text
generated/kotlin/
```

The generator must emit deterministic Kotlin types for route, event, state, view-state, motion, and token contracts.
