{
  "name": "reader-ui-interaction-inventory",
  "version": "1.0.0",
  "description": "Reader-UI Interaction Inventory: IC0 audit + canonical control identity registry + drift tests",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "generate": "node generate-control-ids.mjs --write",
    "check": "node generate-control-ids.mjs --check"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
