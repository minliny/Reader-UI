{
  "name": "reader-ui-contract-tests",
  "version": "1.0.0",
  "description": "Reader UI Contract 校验测试：schema 自检 + fixtures 校验 + codegen 输出一致性",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test ./*.test.mjs",
    "validate": "node ./validate.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
