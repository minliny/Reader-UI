# 页面规划审计结论

审计日期：2026-06-13
复审日期：2026-06-25

> 2026-06-25 复审结论：本文件保留 2026-06-13 的页面规划问题背景，但当前执行源头已切换为 `frontend-demo/`。主导航以 demo 为准：书架 / 发现 / RSS / 设置；书源管理归入设置链路，不再设置“我的”主 Tab。

## 1. 当前主 TAB

仓库当前文档和 demo 已收敛到同一套主 TAB 口径：

| 来源 | 主 TAB 口径 | 当前判断 |
| --- | --- | --- |
| `frontend-demo/` | 书架 / 发现 / RSS / 设置 | 当前可执行 demo 源头 |
| `docs/cross-platform-ui/*`、`docs/ui-handoff/*` | 书架 / 发现 / RSS / 设置 | 已按 demo 口径收敛 |

按本次复审后的范围口径：

- 当前主入口：书架、发现、RSS、设置。
- 搜索是书架/发现等页面内入口，不作为主 Tab。
- 书源管理归入设置链路，不作为主 Tab。
- 不规划“我的”主 TAB。

因此，当前页面规划必须统一为：

```text
主 Tab：书架、发现、RSS、设置
页面内入口：搜索、书籍详情、阅读器
设置链路：书源管理、同步备份、阅读设置、隐私权限、关于反馈
废弃入口：我的、独立书源主 Tab
```

## 2. 页面规划总体结论

当前页面规划和可执行 demo 已经形成主结构闭环，但不能判定为“全应用最终交付完成”。

主要原因：

1. 当前 demo 已覆盖 4 个主 Tab，但 `render.js` 和 `styles.css` 仍是大文件，后续实现前需要拆组件。
2. 发现、RSS、设置已是当前主导航，深层页面仍需要按功能链路继续补足。
3. 搜索、书籍详情、阅读器控制层、书源管理等不再影响主 Tab 口径，但仍是交付链路缺口。
4. 旧编号图片规划只能作为历史参考，当前验收以 demo、manifest 和 handoff 结构为准。
5. 页面级 token、组件、交互和人工审核仍需要在进入平台实现前逐项闭合。

## 3. 49/50 系列还差什么

49/50 系列是当前书架、搜索、详情链路的高优先级缺口。它们不是“已有图片缺文字稿”的问题，而是规划页面/状态尚未形成可用图片 source。

### 49 系列：书架状态与操作

还差 4 张：

| 编号 | 页面或状态 | 当前缺口 |
| --- | --- | --- |
| 49 | 书架空状态 | 需要保留书架标题栏、分组栏和主入口结构，提供导入本地书、搜索书籍等首要动作 |
| 49-1 | 书籍操作底表 | 需要覆盖继续阅读、查看信息、刷新目录、缓存、移动分组、导出、移出书架 |
| 49-2 | 书架排序/筛选 | 需要覆盖排序方式、升降序、筛选条件和应用/取消行为 |
| 49-3 | 书架分组管理 | 需要覆盖分组列表、新增、重命名、排序、删除、移动书籍后的状态 |

49 系列完成标准：

- 每张图必须是单一页面或单一状态，不使用拼图。
- 必须使用当前暖白、紧凑、管理型视觉语言。
- 必须有中文文字稿，说明入口、返回、组件、交互、状态、失败反馈。
- 必须明确是否属于 P0、P1 或暂缓。

### 50 系列：搜索与详情状态

还差 5 张：

| 编号 | 页面或状态 | 当前缺口 |
| --- | --- | --- |
| 50-0 | 搜索 loading | 搜索结果页骨架、搜索框保留、取消或返回行为 |
| 50-1 | 搜索 empty | 无结果说明、修改关键词、切换来源、返回搜索首页 |
| 50-2 | 搜索 error | 请求失败、重试、切换来源、保留查询词 |
| 50-3 | 书籍详情 loading | 顶栏保留、封面/标题/目录预览骨架 |
| 50-4 | 书籍详情 error / 来源失效 | 区分来源失效、网络失败、书籍不存在，不能直接用全屏空状态替代 |

50 系列完成标准：

- 搜索状态不能混同书架状态。
- 详情失败不能直接显示“书籍不存在”，必须区分来源失效和网络异常。
- loading、empty、error 必须保留页面上下文，不得跳到通用孤立状态页。
- 每张图必须配套中文文字稿。

## 4. 35-39 还差什么

`35` 至 `39` 对应阅读外观链路。当前问题不是“完全没有图”，而是现有图不能作为最终 source。

| 图片 | 规划页面 | 当前问题 | 还差什么 |
| --- | --- | --- | --- |
| `35-reader-appearance-font-selection-standard.png` | 字体选择 | 缺少规格要求的字体管理内容，视口不符合目标 | 需要补字体分组、导入、管理、不可用字体、示例预览，并重出标准视口图 |
| `36-reader-appearance-theme-selection-standard.png` | 主题选择 | 主题卡缺正文预览，视口不符合目标 | 需要每个主题卡展示正文预览、当前选中态、自定义主题入口，并重出标准视口图 |
| `37-reader-appearance-theme-edit-standard.png` | 主题编辑 | 内容基本完整，但 final token 未冻结，视口门禁未过 | 需要按最终 token 重审颜色、对比度、保存/取消/重置状态 |
| `38-reader-appearance-layout-advanced-standard.png` | 版式高级 | 内容基本完整，但 final token 和目标视口未闭合 | 需要按目标视口重审页边距、行高、段距、缩进、预览区 |
| `39-reader-appearance-page-turn-animation-standard.png` | 翻页动画 | 存在 iOS 状态栏、视口不符、token 未冻结 | 需要 Android 标准状态栏、安全区、减少动态选项和真实行为说明 |

35-39 完成标准：

- 必须重申它们属于“界面/外观”，不属于“阅读设置”。
- 必须使用 Android 手机 `390×884` 逻辑基准。
- 必须去除 iOS 状态栏或非 Android 系统元素。
- 必须通过 final token、页面级人工审核和 source mapping。
- 必须配套中文文字稿。

## 5. 发现、RSS、设置的范围修正

### 发现

当前处理：一级主 Tab。

发现首页已经纳入当前主导航。后续分类、榜单、来源检测等深层页面按发现链路继续补足。

### RSS

当前处理：一级主 Tab。

RSS 列表已经纳入当前主导航。订阅管理、订阅详情、RSS 搜索等属于 RSS 深层链路，按后续页面闭合推进。

### 设置

当前处理：一级主 Tab。

设置首页已经纳入当前主导航。App 设置二级页、同步备份、缓存、隐私权限、关于反馈、书源管理和阅读设置均归入设置链路继续闭合。

## 6. 中文文稿和中文文件名要求

后续新增审计、规划、文字稿文件应使用中文文件名和中文正文。

建议命名规则：

| 类型 | 中文文件名示例 |
| --- | --- |
| 页面规划审计 | `页面规划审计结论.md` |
| 图片文字稿总册 | `图片逐图文字稿总册.md` |
| 页面 source 映射 | `页面来源映射表.md` |
| 人工视觉审核队列 | `人工视觉审核队列.md` |
| 缺失页面文字稿 | `缺失页面中文文字稿.md` |
| 上传前审计 | `上传前审计报告.md` |

已有英文文件名不应直接批量改名，除非同步更新所有引用、脚本、清单和交叉链接。否则会制造新的断链。

## 7. 最终判断

当前页面规划审计结果：

```text
主入口范围：已统一为书架、发现、RSS、设置
demo 源头：已定位到 frontend-demo/
handoff / cross-platform：已按 demo 口径收敛
搜索 / 书源管理：页面内或设置链路，不作为主 Tab
剩余风险：demo 大文件、深层页面闭合、平台实现映射
```

下一步应围绕 demo 结构拆分组件和平台实现映射推进，而不是继续按旧图片编号判断完成度。
