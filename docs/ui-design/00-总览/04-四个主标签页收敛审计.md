# 四个主 TAB 收敛审计

审计日期：2026-06-13
复审日期：2026-06-25

> 2026-06-25 复审结论：当前执行源头以 `docs/ui-design/frontend-input/frontend-demo-draft` 为准。四个主 Tab 均属于当前主导航；优先级差异只影响深层页面补齐顺序，不再改变主 Tab 结构。

## 1. 当前四个主 TAB

当前 App 主 TAB 名称保留为：

```text
书架 / 发现 / RSS / 设置
```

四个 TAB 均进入当前主导航。本轮按页面级收敛后的范围是：

| 主 TAB | 当前优先级 | 当前处理 |
| --- | --- | --- |
| 书架 | P0 | 当前优先收敛书架、搜索、详情和阅读入口 |
| 发现 | P0 shell / P1 深层 | 主入口保留，分类、榜单等深层页面后续收敛 |
| RSS | P0 shell / P1 深层 | 主入口保留，订阅管理、详情、RSS 搜索后续收敛 |
| 设置 | P0 shell / P1 深层 | 主入口保留，书源管理、同步备份、阅读设置等归入设置链路 |

`我的` 和 `书源` 不再作为主 TAB。书源管理归入设置/管理链路。

当前四个主标签页根页面的约束、图片示例和页面包已整理到：

```text
docs/ui-design/02-主标签页/
```

该目录只收敛底部主导航一级页面；书籍搜索、书籍详情、阅读器、书源管理和设置二级页仍按各自功能链路单独收敛。

## 2. 书架 TAB

当前等级：`P0`

### 当前页面范围

| 页面/状态 | 当前图稿 | 当前文字稿 | 收敛状态 |
| --- | --- | --- | --- |
| 书架封面模式 | `01-书架首页-封面模式.png` | 已有 | 候选可用，页面级未闭合 |
| 书架列表模式 | `02-书架首页-列表模式.png` | 已有 | 候选可用，页面级未闭合 |
| 书架空状态 | 缺 `49` | 已有规划 | 缺图 |
| 书籍操作底表 | 缺 `49-1` | 已有规划 | 缺图 |
| 排序/筛选 | 缺 `49-2` | 已有规划 | 缺图 |
| 分组管理 | 缺 `49-3` | 已有规划 | 缺图 |
| 本地书导入 | 无专用 source | `bookshelf.md`、`page-flows.md` 有规划 | 需要补图或明确降级 |
| 搜索首页 | `03-书籍搜索-首页.png` | 已有 | 候选可用，页面级未闭合 |
| 搜索结果 | `04-书籍搜索-结果页.png` | 已有 | 候选可用，页面级未闭合 |
| 搜索 loading / empty / error | 缺 `50-0` 至 `50-2` | 已有规划 | 缺图 |
| 书籍详情 | `05-书籍详情-默认页.png` | 已有 | 候选可用，页面级未闭合 |
| 详情 loading / error | 缺 `50-3`、`50-4` | 已有规划 | 缺图 |

### 当前结论

书架 TAB 是当前优先收敛的主入口，但还不能判定完整交付。核心缺口是搜索、详情、空状态、底表和本地导入等链路闭合。

最小完成条件：

1. 补齐 `49` 至 `49-3`。
2. 补齐 `50-0` 至 `50-4`。
3. 保留 `01/02/05/06/07` 为候选，并补页面级组件、交互、token 和人工审核。
4. 明确本地书导入是否 P0 完成项；若是，需补专用 source。

## 3. 发现 TAB

当前等级：`P0 shell / P1 深层`

### 当前页面范围

| 页面/状态 | 当前图稿 | 当前文字稿 | 收敛状态 |
| --- | --- | --- | --- |
| 发现首页 | `41-1-discovery-source-category.png` | 已有 | 主入口候选，需按 demo shell 继续闭合 |
| 旧发现首页 | `41-discovery-default.png` | 有历史说明 | 旧编号，不能作为当前 source |
| 信息源选择 | 无当前 source | 有后续规划 | 深层链路待补 |
| 更多分类 | 无当前 source | 有后续规划 | 深层链路待补 |
| 榜单 / 分类详情 | 无当前 source | 有后续规划 | 深层链路待补 |
| 来源检测 | 无当前 source | 有后续规划 | 深层链路待补 |

### 当前结论

发现 TAB 已进入当前主导航。`41-1` 可以作为发现首页候选，但发现状态和次级页仍需按深层链路继续补足。

收敛动作：

1. 所有旧文档中把发现排除出主导航的地方需要改为当前主入口。
2. `41-discovery-default.png` 降级为旧参考。
3. 后续若恢复发现，需要重新定义发现首页是否仍使用 `41-1`。

## 4. RSS TAB

当前等级：`P0 shell / P1 深层`

### 当前页面范围

| 页面/状态 | 当前图稿 | 当前文字稿 | 收敛状态 |
| --- | --- | --- | --- |
| RSS 首页 | `42-rss-home.png` | 已有 | 主入口候选，需按 demo shell 继续闭合 |
| RSS 搜索 | 无当前 source | 有后续规划 | 深层链路待补 |
| 订阅管理 | 无当前 source | 有 route 和后续规划 | 深层链路待补 |
| 添加订阅源 | 无当前 source | 有后续规划 | 深层链路待补 |
| RSS 详情 | 无当前 source | route 中存在 | 深层链路待补 |
| RSS loading / empty / error | 无当前 source | 可复用后续状态规划 | 深层链路待补 |

### 当前结论

RSS TAB 已进入当前主导航。`42` 可以作为 RSS 首页候选，但不代表 RSS 订阅、详情、搜索和状态链路完成。

收敛动作：

1. 旧文档中把 RSS 排除出主导航的地方需要改为当前主入口。
2. route 可保留，深层页面标注为后续范围。
3. 不用 `51` 组作为 RSS 状态模板，因为 `51` 组已人工 FAIL/EXCLUDE。

## 5. 设置 TAB

当前等级：`P0 shell / P1 深层`

### 当前页面范围

| 页面/状态 | 当前图稿 | 当前文字稿 | 收敛状态 |
| --- | --- | --- | --- |
| 设置首页 | `43-settings-home.png` | 已有 | 图片级 PASS，页面级未闭合 |
| App 通用设置 | `48-app-settings-general.png` | 已有 | 人工 FAIL/EXCLUDE |
| 书架与搜索设置 | `48-1-app-settings-bookshelf-search.png` | 已有 | 人工 FAIL/EXCLUDE |
| 缓存管理 | `48-2-app-settings-cache.png` | 已有 | 人工 FAIL/EXCLUDE |
| 同步与备份 | `48-3-app-settings-sync-backup.png` | 已有 | 人工 FAIL/EXCLUDE |
| 隐私与权限 | `48-4-app-settings-privacy-permissions.png` | 已有 | 人工 FAIL/EXCLUDE |
| 关于与反馈 | `48-5-app-settings-about-feedback.png` | 已有 | 人工 FAIL/EXCLUDE |
| 书源管理首页 | `44-source-management-home.png` | 已有 | 图片级 PASS，页面级未闭合 |
| 书源详情/新增/编辑/检测/日志 | `47` 至 `47-6` | 已有 | 图片级 PASS，页面级未闭合 |
| 阅读设置首页和子页 | `46` 至 `46-5` | 已有 | 图片级 PASS，页面级未闭合 |

### 当前结论

设置 TAB 已进入当前主导航。入口页 `43` 可以保留，但 `48` 组不可用。设置链路要完成，必须重新处理 App 设置二级页，并把书源管理、阅读设置作为设置链路下的功能页收敛。

最小完成条件：

1. 保留 `43` 为设置首页候选。
2. 重画或重新设计 `48` 组，不得沿用人工 FAIL 的 source。
3. 明确设置下的子功能层级：App 设置、书源管理、阅读设置、同步备份、隐私权限、关于反馈。
4. 给设置链路补正式中文 source 映射表。

## 6. 主 TAB 复审清单

| 文件 | 2026-06-25 复审状态 | 后续动作 |
| --- | --- | --- |
| `docs/cross-platform-ui/CROSS_PLATFORM_ROUTE_MATRIX.md` | 已收敛为书架、发现、RSS、设置 | 持续跟随 demo |
| `docs/cross-platform-ui/CROSS_PLATFORM_UI_BASELINE.md` | 已删除旧主入口 | 持续跟随 demo |
| `docs/cross-platform-ui/CROSS_PLATFORM_COMPONENT_MAPPING.md` | 已收敛为当前主底栏 | 持续跟随 demo |
| `docs/ui-handoff/SCREEN_MATRIX.md` | 已改为设置分组和当前主 Tab | 持续跟随 demo |
| `docs/ui-design/91-历史归档/planning/UI设计完成计划/UI_DESIGN_COMPLETION_PLAN_V1/07_FIGMA_FRAME_PLAN.md` | 历史归档 | 不作为当前源头 |
| `docs/ui-design/91-历史归档/planning/UI设计完成计划/UI_DESIGN_COMPLETION_PLAN_V1/08_FREEZE_SEQUENCE.md` | 历史归档 | 不作为当前源头 |

## 7. 主 TAB 收敛顺序

1. 以 `frontend-demo-draft` 继续拆分四个主 Tab 的 shell 和页面组件。
2. 先闭合书架、搜索、详情和阅读入口。
3. 再补发现、RSS 和设置深层页面。
4. 将书源管理、同步备份、阅读设置、隐私权限和关于反馈固定在设置链路下。
5. 历史归档只作参考，不反向覆盖 demo 口径。

## 8. 最终判断

```text
书架：当前主 Tab，优先闭合
发现：当前主 Tab，深层页面待补
RSS：当前主 Tab，深层页面待补
设置：当前主 Tab，设置链路待补
我的：不设主 Tab
书源管理：设置链路功能页
```
