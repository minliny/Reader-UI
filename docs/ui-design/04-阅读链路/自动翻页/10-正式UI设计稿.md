# 自动翻页正式 UI 设计稿

状态：正式稿
母版：`../08-阅读派生模块继承视觉规格.md`

<!-- LOCAL_DEMO_STYLE_ANCHOR -->
## 本地 Demo 对齐
- 当前视觉以本地 `/frontend-demo/?captureRoute=auto-page` 运行态为准。
- 顶层 `UI设计图.png` 已由当前 demo 截图生成；旧 `图片/` 候选图只保留为历史参考。
- 结构、比例、控件位置、字体、颜色和图标约束如与旧文字描述冲突，以本节和当前 demo 为准。
<!-- /LOCAL_DEMO_STYLE_ANCHOR -->
## 定义范围

本页面包定义自动翻页设置覆盖层、运行胶囊和完整设置页。自动翻页不负责朗读。

## 结构

- `Overlay`：速度、翻页方式、屏幕常亮、章末停止、音量键调速、取消、开始自动翻页。
- `RunningCapsule`：暂停/继续、关闭。
- `FullPage`：默认速度、翻页方式、运行行为、触发区域、预览。

## 继承规则

- 设置覆盖层不改变阅读正文排版。
- 运行态只使用极简胶囊。
- 完整设置页不显示阅读控制层底部模块导航。

## 图片输入

优先图片：

- `图片/01-自动翻页设置覆盖层.png`
- `图片/02-自动翻页运行胶囊.png`
- `图片/03-自动翻页设置页.png`

## 禁止项

- 不显示复杂速度工具条或底部停靠栏。
- 不把朗读控制混入自动翻页。
- 不在运行态显示底表。
